# C.T.S

## Buy Rules Separate

File: `pine/Buy_Rules_Separate.pine`

Each rule is independent:
- own condition
- own BUY arrow (`R1` … `R32`)
- own label `🟢 BUY Rn`
- own alert `🟢 BUY Rn`

### Rules
1–16, 19–23, 25, 27–32

### Single-rule mode
Settings → enable **تشغيل قاعدة واحدة فقط** → pick rule number.
